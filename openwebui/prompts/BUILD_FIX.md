Treat this as an engineering/build task.

Inspect the current state first. Reuse working components. Find the smallest root blocker, fix one bounded thing at a time, and verify after each change.

Do not rewrite an entire working system to solve a narrow defect.
Do not hide errors.
Return exact failure messages where relevant.
Prefer a working tested implementation over a long explanation.
